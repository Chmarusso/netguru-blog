.row
  .span6
    = simple_form_for Comment.new, html: { class: "form-inline" } do |f|
      = f.input :post_id, :as => :hidden, :input_html => { :value => post.id }
      = f.input :body, :label => false
      = f.button :submit, :class => "btn-primary"